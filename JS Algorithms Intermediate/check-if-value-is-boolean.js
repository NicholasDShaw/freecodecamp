// check if value is boolean
function booWho(bool) {
  // What is the new fad diet for ghost developers? The Boolean.
  bool = (bool === true || bool === false) ? true : false;
  return bool;
}

booWho(null);
